name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:  # Manual trigger

jobs:
  publish-moda:
    runs-on: ubuntu-latest
    environment: pypi-moda
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Set up Python
        run: uv python install 3.12

      - name: Build moda
        working-directory: packages/moda
        run: uv build

      - name: Publish moda to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/moda/dist/

  publish-moda-openai:
    runs-on: ubuntu-latest
    environment: pypi-openai
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Set up Python
        run: uv python install 3.12

      - name: Build moda-openai
        working-directory: packages/moda-openai
        run: uv build

      - name: Publish moda-openai to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/moda-openai/dist/

  publish-moda-anthropic:
    runs-on: ubuntu-latest
    environment: pypi-anthropic
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Set up Python
        run: uv python install 3.12

      - name: Build moda-anthropic
        working-directory: packages/moda-anthropic
        run: uv build

      - name: Publish moda-anthropic to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/moda-anthropic/dist/
