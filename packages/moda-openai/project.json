{
  "name": "moda-openai",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/moda-openai/opentelemetry/instrumentation/openai",
  "targets": {
    "lock": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv lock",
        "cwd": "packages/moda-openai"
      }
    },
    "add": {
      "executor": "@nxlv/python:add",
      "options": {}
    },
    "update": {
      "executor": "@nxlv/python:update",
      "options": {}
    },
    "remove": {
      "executor": "@nxlv/python:remove",
      "options": {}
    },
    "build": {
      "executor": "@nxlv/python:build",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "outputPath": "packages/moda-openai/dist",
        "publish": false,
        "lockedVersions": true,
        "bundleLocalDependencies": true
      }
    },
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv sync --all-groups",
        "cwd": "packages/moda-openai"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff check .",
        "cwd": "packages/moda-openai"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/reports/packages/moda-openai/unittests",
        "{workspaceRoot}/coverage/packages/moda-openai"
      ],
      "options": {
        "command": "uv run pytest tests/",
        "cwd": "packages/moda-openai"
      }
    },
    "build-release": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "chmod +x ../../scripts/build-release.sh",
          "../../scripts/build-release.sh"
        ],
        "cwd": "packages/moda-openai"
      }
    }
  },
  "tags": [
    "instrumentation"
  ]
}
